---
name: srt-whiteboard-animation
description: SRT 字幕を温かみのあるベージュ紙地のホワイトボード手描きアニメにする：字幕を読む→挿絵の方針を出力→承認後に統一スタイルの線画を生成→物語の意味に沿って領域をアノテーション→プレビューツールで調整→MP4 レンダリング。演出構成は領域マスクによる出現（annotation.json / sequence / startMs / protectedRegions）を踏襲しつつ、各領域内の描線は stream 方式の連続した筆跡（骨格／グリッド ink→color）に置き換える。ユーザーが SRT 字幕を提供して「字幕からホワイトボード手描き／流れる筆跡の動画にして」「SRTから白板手描き動画を作る」「SRT字幕をホワイトボードアニメにする」「字幕に沿ってシーン割りして手描きで描いて」などと求めたときに発動する。
---

# SRT ホワイトボードアニメ（mask 構成 + stream 描画）

SRT 字幕をホワイトボード手描きアニメに変換する：**演出構成**は領域マスクによる出現を踏襲し（物語の順に領域を 1 つずつ出現させ、開始前の領域は完全に隠し、重なりは `protectedRegions` で保護する）、**描画方法**は流れる筆跡に置き換える——各領域は自身の許可マスクの内側で、ペン先が骨格／グリッドに沿って連続的に滑りながら描線する（描き起こし ink → 着色 color）。すべての領域は 1 枚の永続キャンバスを共有し、描き終えた領域はキャンバス上に残る。ユーザー向けの説明・絵コンテ・設定・UI テキストはすべて日本語を使用しなければならない。

コマ単位のジャンプや矩形のワイプ消去とは異なり、この skill の筆跡は**滑らかに連続して流れる**。また画像全体を一括で描く stream とも異なり、この skill は**字幕の物語単位で領域を分割**して順に描くため、各要素の登場順とタイミングを制御できる。

## デフォルト実装パラメータ

| 項目 | デフォルト要件 |
|---|---|
| 紙の背景 | 生成画像は温かみのあるベージュの古紙色を使う（推奨 `#F5EBD7`）。レンダリング時は元画像の四隅から内側にオフセットした位置をサンプリングして地色を作る。純白は禁止。 |
| 描画方法 | 各領域は stream の連続筆跡：描き起こし `ink`（線画を敷く）→ 着色 `color`（元の色を復元）。比率は `ink:color = 2:1`。 |
| 筆跡パス | `--ink-path grid`（グリッド。デフォルトで安定）または `skeleton`（骨格トレース。線画が明瞭なイラストによく馴染む）。 |
| 着色スタイル | `--color-fill contour-wipe`（輪郭スキャン。デフォルト）または `brush`（軌跡に沿って塗る）。 |
| 未描画領域 | 領域の許可マスク = 矩形 `region` から「後続の領域 + protectedRegions」を差し引いたもの。開始前の領域は完全に非表示。 |
| 尺の決定元 | 各画像の `sceneDurationMs` は、そのシーンに対応する字幕の時間幅から決める（推奨 25〜35 秒／シーン）。 |
| 編集ボックス | プレビューツールは既定ですべての番号付き編集ボックスを表示する。編集ボックスはアニメーション画面の内容ではない。 |

## 統一ビジュアル仕様（必須）

すべてのシーンの元画像は同一のビジュアル言語に従わなければならない。生成前に以下の要件を画像生成プロンプトへ漏れなく書き込み、生成後に 1 項目ずつ確認する。

- **スタイルと構図：** ミニマルな手描きイラスト、純粋なスケッチ風、Notion のような抑制の効いた落書き的な美意識。写実性ではなく概念の表現を主とする。構図は簡潔、背景はすっきり、余白を大きく取り、全体の情感は穏やかで明快。シリーズ内で線・人物・配色を統一する。
- **色と質感：** ベージュの紙背景 `#F5EBD7`、濃いグレーのスケッチ線。赤・オレンジ・青のみ、概念的なアクセントとして少量使用してよい。それ以外のアクセントカラー、高彩度の配色、複雑なテクスチャは使わない。
- **人物とオブジェクト：** 対象は簡潔な輪郭・少ない線・余白で表現し、実際の比率や質感・ディテールではなく、関係性／変化／中心概念を強調する。
- **絶対禁止：** シーン元画像内のあらゆる文字・単語・アルファベット・数字・フォント・ラベル。写実感、写真的なディテール、3D 効果、絵画的な質感。複雑なシーン、密度の高い背景、過剰な装飾、高彩度の画面。
- **描画する手の例外：** ペン軸の文字が自分のロゴであり残したいとユーザーが明示した場合に限り、`drawing-hand.png` のペン軸のロゴを残してよい。これはシーン元画像の文字には当たらず、消去や描き直しも不要。ユーザーからの明示がない場合は、従来どおり文字なしの画面として扱う。

## 確認ゲート（必須）

デフォルトのワークフローでは、**各ステップの完了後に必ず停止し、ユーザーの明確な確認を待ってから**次のステップに進む。確認前に次のステップの画像・アノテーション・プレビュー・動画・結合ファイルを生成してはならない。「返信がない」「以前に受けた包括的な許可」「ユーザーが反対しなかった」ことを確認とみなしてはならない。ユーザーが前のステップの修正を求めた場合は、そのステップだけをやり直し、完了後に再び確認を待つ。

唯一の連動アクションは次のとおり：**アノテーション JSON の作成が完了したら、直ちにプレビューツールを自動で開き、その JSON があるディレクトリを読み込ませる**こと。これはステップ 3 の成果物に含まれるため、「プレビューツールを開く」ために別途確認を待つ必要はない。ブラウザの File System Access API がユーザー操作を要求する場合は、ブラウザの UI からこの確定済みディレクトリを選択する。そのことを理由にユーザーへ追加の確認を求めたり、ユーザー自身にプレビューツールを開かせたりしてはならない。

## ワークフロー

1. **字幕を読み、方針を出す（画像は生成しない）。** `scripts/parse_srt.py` で SRT を字幕エントリに解析し、25〜35 秒／シーンでシーン割り案を出す。それに基づいて挿絵の方針を出力する：各シーンの場面番号、中心となる表現、画面の主体、対応する字幕区間、`sceneDurationMs`。各シーンは 1 つの中心的な意味だけを表現する。**完了後は停止し、ユーザーの方針確認を待つ。**
2. **線画を生成する。** ユーザーが方針を確認した後にのみ、「統一ビジュアル仕様」に従ってシーンごとに 16:9 の温かいベージュ古紙地の線画を生成する。背景は `#F5EBD7`、主体どうしの間は自動分割しやすいよう十分な余白を空ける。文字・複雑な写真・重なり合うオブジェクト・仕様に反する要素を生成してはならない。**完了後は停止し、線画を提示してユーザーの確認を待つ。**
3. **まず字幕を読み、次に画像を見て、アノテーションしてプレビューツールを開く。** ユーザーが線画を確認した後にのみ、まずその画像に対応する字幕を読み、次に実際に画像を見て、元画像のピクセル幅・高さを取得する。字幕だけで画面を推測してはならず、画面上の位置だけで機械的に並べてもならない。まず字幕から物語上の出来事を抽出し、次に画像内で見えている主体を各出来事に対応させ、「場面設定 → 鍵となる人物／物体 → 動作の衝突または変化 → 反応／結果」という意味的な順序で描画順を決める。続いて `<画像名>.annotation.json` を作成する。作成が完了したら直ちに既定のブラウザで `assets/preview.html` を開き、プレビューツールの「フォルダを開く」から**そのアノテーションファイルがあるディレクトリ**の `<名前>.png` + `<名前>.annotation.json` をすべて読み込む。ファイルパスを提示するだけ、あるいはユーザーに操作させるのは不可。**プレビューツールにディレクトリを読み込ませたら停止し、アノテーションとプレビュー内容の確認を待つ。**
4. **領域プレビュー画像を生成する。** ユーザーがアノテーションとプレビュー内容を確認した後にのみ、`render_annotation_preview.py` で番号／方向のチェック画像を出力し、領域分割が物語順と一致しているか、領域がすべてキャンバス内に収まっているか、重なる主体が `protectedRegions` で保護されているかを照合する。**完了後は停止し、ユーザーのプレビュー画像確認を待つ。**
5. **プレビューツールで調整して保存する。** ユーザーがプレビュー画像を確認した後にのみ、既に開いていて対応ディレクトリを読み込み済みのプレビューツールで調整する：既定（未再生）では画像全体と領域枠を表示する。キャンバスは**矩形のプロキシ**であり、領域の四辺・四隅をドラッグして `region` を変更、右側で名称／方向／**開始 (ms)／終了 (ms)**（尺 = 終了 − 開始、読み取り専用）と**字幕**を変更、モジュール一覧をドラッグして**順序を変更**（`sequence` を自動で振り直す）、モジュールを選択すると対応する字幕が自動でハイライトされる。タイムラインをドラッグするか再生ボタンで出現の様子を確認できる（開始前の領域は表示されない）。`direction` はこのプロキシにのみ影響する。編集後に「このシーンを保存／すべて保存」をクリックすると元の `.annotation.json` に書き戻される（各領域の `subtitle` を含み、`sceneDurationMs` は最後の領域の終了 +0.5s に揃えられる）。**保存後は停止し、最終アノテーションとタイミングの確認を待つ。**
6. **コマンドラインで完成動画をレンダリングする。** ユーザーが最終アノテーションとタイミングを確認した後にのみ、`render_stream_whiteboard.py` でシーンごとにフル画質の MP4 を出力し、冒頭・重なりのあるモジュールの中盤・末尾の 3 つの時点を抜き取りチェックする。**完了後は停止し、ユーザーの完成動画確認を待つ。**
7. **複数シーンの結合（複数シーンの場合のみ）。** ユーザーがすべての単一シーン完成動画を確認した後にのみ、`merge_scenes.py` で順番どおりに 1 本へ結合する。**完了後は停止し、ユーザーの最終合成動画確認を待つ。**

## ディレクトリ規約

ユーザーのプロジェクト内に作成する：

```text
assets/whiteboard/<プロジェクト名>/
  scene-01-<名称>.png
  scene-01-<名称>.annotation.json     # png と同名
  scene-01-<名称>-whiteboard.mp4      # 完成動画
  scene-01-<名称>-preview.mp4         # 実プレビュー映像（プレビューツールが生成、低画質）
```

画像と設定は同名でなければならない：`foo.png` には `foo.annotation.json` が対応する。プレビューツールはこれを手がかりに設定を自動読み込みする。

## 意味的な順序づけとピクセル単位のアノテーション（必ず実行）

1. **参照の根拠：** アノテーション前に、字幕と実際に閲覧済みの元画像の両方が揃っていなければならない。どちらかが欠けている場合は先に入手する。欠けたままアノテーションを生成してはならない。
2. **順序の根拠：** `sequence`、`startMs`、`label` は字幕中の出来事の前後関係を反映しなければならない。左から右、上から下、あるいは視覚的な目立ちやすさだけで決めてはならない。
3. **座標の根拠：** 各モジュールは元画像の座標系における整数ピクセルの `x`、`y`、`width`、`height` を出力する。原点は左上。パーセント／比率／推定座標や、サイズの省略は禁止。`canvas.width` / `canvas.height` は元画像のピクセルサイズと一致しなければならない。
4. **モジュールのフィールド：** 各要素は `sequence`、`narrativeRole`、`subtitle`、`region`、`reveal`、`handPath` を持つ。`narrativeRole` は字幕における物語上の役割を日本語で記述する。`subtitle` はその領域に対応する字幕テキストを保持する（SRT 由来。プレビューツールの連動や後続用途のため）。`sequence` は 1 から始まる連番。
5. **検証：** プレビュー生成前に、各領域がキャンバス内にあるか、対応する可視の主体を覆っているか、字幕の出来事と一致しているかを確認する。重なる主体は `protectedRegions` で保護してからモジュールを描画する。

## タイミングモデル（stream 描画専用）

- **各シーンの総尺** `sceneDurationMs` は、そのシーンの字幕の時間幅に由来する（`parse_srt.py` の `scenes[].sceneDurationMs`）。
- **領域は直列で描く：** stream 描画は 1 本のペンが動く方式なので、同一シーン内の各領域は**時間的に順次進行**させる（`startMs` を重ねない）。次の領域は前の領域の `startMs + durationMs`（+ 任意で 100〜300ms の間）から開始する。`startMs` が重なっている場合、レンダラーは依然として順番に処理するが、見た目としては並行描画にはならない。
- **領域内の ink→color：** 各領域の `durationMs` は `ink:color = 2:1` で描き起こし区間と着色区間に分割される。`durationMs` はプレビューツールの**開始／終了時刻**で決まり（終了 − 開始）、その領域に対応する字幕の長さに揃えられる。初期見積もりとして 150 ピクセル/秒 × 描画距離を使ってもよい。
- **余韻の締め：** すべての領域を描き終えると自動的に `sceneDurationMs` まで補完され、末尾で最低 0.5 秒は完全な元画像が表示されることが保証される。
- `reveal.direction` は stream 描画では**実際の筆跡を決定しない**（筆跡は骨格／グリッドから自動生成される）。プレビューツールの矩形プロキシのデモ用途のみであり、プレビューツールを使えるようにするために残してある。

## マスクの不変条件（構成レイヤー。必ず実行）

- 時刻 `t` において、モジュールは `reveal.startMs ≤ t` を満たし、かつ現在の描画進行を超えないピクセルのみを表示できる。開始前のモジュールの線・塗り・画像はいっさい現れてはならない。
- 各領域の**許可マスク** = 矩形 `region` から**後続すべてのモジュールの `region`** を差し引き、さらに当該モジュールの `reveal.protectedRegions` を差し引いたもの。stream の描線は許可マスク内に制限されるため、後続領域の線が先に現れることはない。
- `protectedRegions` は `region` と同じく元画像の整数ピクセル座標を用い、矩形が大きすぎる場合、主体が重なる場合、背景の線が漏れ出しうる場合に使う。
- レンダラーは「許可マスク内に制限して描線する → 後続領域と保護領域には自然に触れない」という順序を既に実装している。プレビューツールの矩形プロキシは等価な `destination-out` による差し引きで同じ構成をデモする。

## 設定例

```json
{
  "sceneId": "scene-01",
  "canvas": { "width": 1672, "height": 941 },
  "storyBasis": "そのシーンの字幕における出来事の要約",
  "sceneDurationMs": 9000,
  "elements": [
    {
      "id": "rockery",
      "label": "築山の場面",
      "sequence": 1,
      "narrativeRole": "物語の場面設定",
      "subtitle": "猿山の上、一匹の子猿が築山のてっぺんに座り、手にはバナナを持っている。",
      "type": "structure",
      "region": { "x": 20, "y": 120, "width": 540, "height": 780 },
      "reveal": { "direction": "top_to_bottom", "startMs": 300, "durationMs": 2600, "maskPaddingPx": 22, "protectedRegions": [] },
      "handPath": { "start": [290, 130], "end": [290, 890], "easing": "easeInOut" }
    }
  ]
}
```

> `direction` / `handPath` はプレビューツールの矩形プロキシ専用。完成動画の筆跡は stream が自動生成するため、細かく調整する必要はない。

## スクリプトの使い方

すべてのレンダリングスクリプトは skill 内の `.venv` のインタープリタで実行する（依存関係の分離のため）。

1. **環境の準備**（初回、または依存関係が足りないとき）：
   ```bash
   python scripts/prepare_env.py --check   # 探索。成功時は最終行に ENV_PY=<パス> を出力するので控えておく
   python scripts/prepare_env.py           # 不足していれば .venv を作り opencv-python/numpy/av を導入
   ```
2. **字幕の解析 + シーン割り案**：
   ```bash
   python scripts/parse_srt.py <字幕.srt> --target-sec 30 --min-sec 25 --max-sec 35
   ```
3. **領域番号のプレビュー画像**：
   ```bash
   python scripts/render_annotation_preview.py <画像> <アノテーション> <プレビュー画像の出力先>
   ```
4. **プレビューツール（サーバー不要）**：Chrome / Edge で `assets/preview.html` を直接開き、「フォルダを開く」からディレクトリを選択 → 全画像 + 同名アノテーションを読み込み → ドラッグで編集 → 「保存」で元ファイルに書き戻す。書き戻しには File System Access API が必要（Chrome/Edge）。他のブラウザではダウンロードして手動で上書きする。レンダリングは引き続きコマンドラインで行う（下記ステップ 5）。
5. **単一シーンの完成動画をレンダリング**：
   ```bash
   <ENV_PY> scripts/render_stream_whiteboard.py <画像> <アノテーション> <出力mp4> assets/drawing-hand.png \
       [--ink-path grid|skeleton] [--color-fill contour-wipe|brush] [--total-ms <ミリ秒>]
   ```
   `--total-ms` を省略した場合はアノテーション内の `sceneDurationMs` を使う。最終行に `OUTPUT=<パス>` を出力する。
6. **複数シーンの結合**：
   ```bash
   <ENV_PY> scripts/merge_scenes.py --inputs シーン1.mp4 シーン2.mp4 シーン3.mp4 --output final.mp4
   ```

## 品質チェック

レンダリングの前後に確認する：

- 最初のフレームがきれいな温かいベージュの古紙地であり、線が先に現れていないこと。
- 対応する字幕を読み、元画像を実際に閲覧済みであること。`canvas` が元画像のピクセルサイズと一致し、すべての `region` が整数ピクセル座標でキャンバス内に収まっていること。
- `sequence`、`startMs` が字幕の出来事の順序と一致していること。プレビュー画像の番号／ラベル／領域が同一のアノテーション JSON 由来であること。
- 冒頭・重なりのあるモジュールの中盤・全モジュール完了後の 3 時点を確認する：未描画モジュールがいずれも見えないこと、重なりの保護領域が漏れていないこと、最終フレームが完全な元画像を表示していること。
- ペン先が進行中の筆跡に密着していること。線画が明瞭なイラストでは `--ink-path skeleton` を使うと筆跡がより馴染む。
- 全モジュール終了後、完全な元画像が最低 0.5 秒表示されること。
- 複数シーンの結合後、順序と尺が字幕のシーン割りと一致していること。

効果を修正したい場合は、まずプレビューツール（`assets/preview.html`）でアノテーション（領域／順序／タイミング）を調整して保存し、その後コマンドラインでレンダリングする。当てずっぽうに何度も出力し直さないこと。
